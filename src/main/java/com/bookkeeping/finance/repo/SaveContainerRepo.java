package com.bookkeeping.finance.repo;

import com.bookkeeping.finance.model.SaveContainer;
import org.springframework.data.repository.CrudRepository;

public interface SaveContainerRepo extends CrudRepository<SaveContainer, Long> {
}
